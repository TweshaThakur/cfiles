#include <stdio.h>
#include <stdlib.h>

// program 1 - cube of first 5 numbers when number is passed as argument in a function, apply loop in main fn during function call

/**int cube(int a);

int main()
{
    int i ;


    for (i=1 ; i<=5 ; i++)
    {
        cube(i);

    }

    return 0;
}

int cube(int a)
{
    static int ctr =0;  // ****storage classes ***
    ctr++;
    printf("\nNo. of time fn is called - %d ", ctr);
    printf("Cube of first 5 numbers is as below %d \n", a*a*a);
    return 0;
}
**/



