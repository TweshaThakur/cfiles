/* program to calculate area and perimeter*/

#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*double height=56.9;
    double width=32.5;
    double area=0.0;
    double perimeter=0.0;

    perimeter=2.0*(height+width);
    area=height*width;


    printf("Width is : %.2f, Perimeter is: %.2f",width,perimeter);
    printf("\n");
    printf("Area is : %.2f", area);
    return 0;*/

    enum company{google,facebook,xerox,yahoo,ebay,microsoft};
    enum company a=xerox;
    enum company b=google;
    enum company c=ebay;

    printf("%i\n%i\n%i",a,b,c);
    return 0;
}
