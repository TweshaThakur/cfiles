#include <stdio.h>
#include <stdlib.h>

int main()
{
    int sum;
    char msg[50];
    FILE *fptr;
    fptr = fopen("summers.txt","w");

// if using read mode , exception - file maybe not found
    if (ftpr==NULL)
    {
        printf("Error opening file!");
        exit(1);
    }


    return 0;
}
