#include <stdio.h>
#include <stdlib.h>

//a module of a code action when called

/** syntax of writing a function (case sensitive)

# always call function to execute it

function definition ---->

return-type/void function-name( formal arguments / parameters )
{

       body;

       return ;

}

call -->

function-name( actual arguments )


function declaration at the top !!!!!  if function declared after main function
main function at the end = do all the function declaring before

function declaration --->

return-type function-name(return-type/ data type of arguments);

local / global variables


**/

int main()
{

    return 0;
}
